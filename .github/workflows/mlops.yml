name: MLOps Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  train-and-evaluate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install mlflow scikit-learn pandas numpy joblib

      - name: Verify MLflow installation
        run: mlflow --version

      - name: Prepare data
        run: |
          mkdir -p data/processed
          cp data/raw/cashflow_prediction_dataset_100k.csv data/processed/cashflow_prediction_dataset_100k.csv

      - name: Run training pipeline
        run: |
          cd src
          python train_pipeline.py

      - name: Run tests
        run: |
          pip install pytest
          pytest tests/ -v

      - name: Upload MLflow tracking data
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-runs
          path: mlruns/
          retention-days: 30

      - name: Upload trained models
        uses: actions/upload-artifact@v4
        with:
          name: trained-models
          path: models/
          retention-days: 30

      - name: Upload evaluation report
        uses: actions/upload-artifact@v4
        with:
          name: evaluation-report
          path: evaluation_report.md
          retention-days: 30

      - name: Print evaluation report
        run: cat evaluation_report.md